import 'package:flutter/material.dart';

class History_Employe extends StatelessWidget {
  const History_Employe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('History'),
      ),
      body: Center(
        child: Text('History Page'),
      ),
    );
  }
}